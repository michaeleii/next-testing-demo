import { render } from "@/lib/render";
import TodoApp from "./todo-app";

describe("Todo App", () => {
  test.todo("Should render the component");

  test.todo("Should add a todo to the todo list");

  test.todo(
    "Should remove a todo when trash button is clicked from a todo item.",
  );
});
